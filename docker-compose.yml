version: "2.0"

services:
  spacedeck:
    build: .
    container_name: spacedeck
    ports:
      - "9666:9666"
    volumes:
      - /Users/user/Desktop/github/spacedeck-open/storage:/app/storage
      - /Users/user/Desktop/github/spacedeck-open/storage/database.sqlite:/app/database.sqlite

